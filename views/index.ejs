<html>
    <head>
        <title><%= title %></title>
    </head>
    <body>
        <h1><%= title %></h1>
        <ul>
            <% messages.forEach((message) => { %>
                <%- include('message', {message: message}); %>
            <% }); %>
        </ul>
        <footer>
            <a href="/new">Post</a>
        </footer>
    </body>
</html>